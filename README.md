# Automation Testing
The automation thesting runs in a local virtual environment.
We chose a virtual environment as there is no need for each team member to install all the dependencies on their globalpyton setups.

## playright 


install --with-deps